import random

import matplotlib.pyplot as plt
import numpy as np
from matplotlib import animation


def init():
    return []


def update(i, field, x, y):
    # ax.collections = []
    # ax.patches = []
    return []


# anim = animation.FuncAnimation(fig, update, fargs=(field, x, y), init_func=init, interval=100, blit=True)
